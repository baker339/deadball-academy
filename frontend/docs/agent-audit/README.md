# Lesson agent audit outputs

Re-running `scripts/run-lesson-agent-audit.ts` (from `frontend/`) regenerates structured scorecards and narrative memos under this directory. Large memo trees are not kept in git by default; commit intentionally if you want them in history, or delete locally.
