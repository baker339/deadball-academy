# Caching

The previous localStorage-based chart API cache (`useCachedData`, `CacheManager`, and related UI) was removed together with the retired analytics and explore chart pages.

For curriculum and CMS workflows, use `npm run sync-cms-runtime` and the learning library routes documented in [README.md](README.md).
